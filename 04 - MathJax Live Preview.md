# 04 - MathJax Live Preview Test

Toggle the settings parameter **Keep loom notes in source mode** off in Loom General settings.

1. Once toggled off, this note should automatically reload into Live Preview.
2. The LaTeX equation below should compile and render visually instead of staying as raw text:

$$
i\hbar\frac{\partial}{\partial t}\Psi(\mathbf{r},t) = \hat{H}\Psi(\mathbf{r},t)
$$

## Helper Run Block
```python
print("Loom is loaded and ready.")
```
